import "./Filters.scss";

const Filters = () => {
  return (
    <section className="section-filters todos-app-container">
      <h2 className="heading-primary">Filter</h2>
    </section>
  );
};

export default Filters;
